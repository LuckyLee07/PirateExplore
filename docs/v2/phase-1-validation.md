# V2 阶段 1 验证报告

> 日期：2026-07-16
>
> 结论：通过

## 1. 自动校验

执行：

```bash
tools/v2/validate_phase1.sh
```

结果：

- 阶段 0 配置、11 张源表、46 条数据和旧系统门禁继续通过；
- 运行时 Lua 数据与 CSV 源表完全一致；
- 全部生产 Lua 文件通过语法检查；
- 安全航线完整闭环通过；
- 风险航线、沉船奖励、诅咒强化和火炮升级闭环通过；
- 甲板破坏从舰炮战传递到接舷战通过；
- 战败、主动撤退、战斗重试和返港恢复通过；
- 非法阶段操作和不兼容存档回退通过；
- V2 主入口、旧菜单隐藏、旧随机事件隔离通过。

## 2. iOS 模拟器

设备：`NewPirate Fresh QA`，iOS 26.2，arm64。

验证结果：

- 构建、安装和启动成功；
- `player` 新档直接显示 V2 瓶中召唤和首个明确目标；
- `qa_combat` 独立档直接显示舰炮战、甲板破坏阈值和三个可选操作；
- 旧顶部经济栏、底部七按钮、钻石入口和随机事件未覆盖 V2 页面；
- 应用持续运行超过 90 秒，无 Lua 运行错误或后台退出；
- 新档与 QA 战斗档互不覆盖。

启动时发现旧音频后端在 iOS 26.2 模拟器播放旧主城音乐会触发 CoreAudio RPC
死锁。阶段 1 尚无获批 V2 音频，因此 V2 模式已隔离旧音乐和按钮音效；正式音频
在阶段 3 接入时必须重新做模拟器与真机验证。

## 3. iOS 设备编译

执行：

```bash
./xcode.sh ios-device
```

结果：arm64、关闭签名的设备编译成功。

## 4. 验收映射

| 阶段 1 门槛 | 证据 | 结论 |
|---|---|---|
| 新存档可无作弊完成首章 | 两条完整状态机测试、player 运行入口 | 通过 |
| 无必须进入旧商店或长期系统的步骤 | V2 独立界面与旧菜单隔离 | 通过 |
| 无口头指导可第一次出航 | 首屏单一目标、默认可出航配置 | 通过，仍需阶段 4 外部用户复核 |
| 全部失败路径可恢复 | 战败/撤退的重试与返港测试 | 通过 |
| 模拟器与设备编译通过 | arm64 模拟器与关闭签名的设备编译均通过 | 通过 |

## 5. 非阻断项

- 当前是工程灰盒，不包含阶段 3 正式构图、角色资源、动画和音效；
- 当前航线和战斗决策深度只满足闭环验证，阶段 2 继续重做；
- 当前测试证明流程可完成，尚不等于真实玩家自然用时达到 15～20 分钟；
- iOS 8 deployment target、LaunchImage、UIScene 和旧引擎警告继续列为发行前技术债。
