# V2 阶段 2 验证报告

> 日期：2026-07-16
>
> 结论：通过

## 1. 自动验证

执行：

```bash
tools/v2/validate_phase2.sh
```

结果：

- 阶段 0、阶段 1 全部回归继续通过；
- 14 张运行时表与源 CSV 导出一致；
- 2 条路线、17 个平衡参数和 7 个战斗动作通过字段与关系检查；
- 航海士测绘前后信息差异和 1 补给成本通过；
- 重炮模块降低补给容量通过；
- 甲板与火炮两种目标、敌炮压制和反击降低通过；
- 炮手标记、水手守卫、医师治疗通过；
- 甲板破坏到接舷状态的传递继续通过；
- 战斗胜因复盘通过；
- 撤退、战败、重试成本和返港恢复成本通过；
- 核心战斗数值散落硬编码检查通过。

## 2. iOS 模拟器

设备：`NewPirate Fresh QA`，iOS 26.2，arm64。

使用 `qa_explore` 与 `qa_combat` 隔离档位验证：

- 未测绘航线只显示方向，测绘动作和补给成本可读；
- 三个探索动作在手机安全区内完整显示；
- 舰炮页面同时显示船体、敌舰、甲板与敌炮状态；
- 五个舰炮动作在一屏内完整显示，无按钮遮挡；
- 目标、规则、最近结果和当前动作层级清楚；
- 应用保持运行，无 Lua 运行错误或音频后台退出。

截图见 `phase-2-explore.png` 和 `phase-2-combat.png`。

## 3. iOS 设备编译

执行：

```bash
./xcode.sh ios-device
```

结果：arm64、关闭签名的设备编译成功。

## 4. 阶段门槛映射

| 阶段 2 门槛 | 证据 | 结论 |
|---|---|---|
| 可解释路线利弊 | 精确风险/成本/收益与自动测试 | 通过，阶段 4 由外部玩家复核 |
| 能说明舰炮如何影响接舷 | 阈值、开场状态和胜因复盘 | 通过 |
| 关键结果均有反馈 | 四状态战斗栏、最近结果、战后复盘 | 通过 |
| 数值通过源表调整 | `route`、`balance`、`battle_action` | 通过 |
| 失败和恢复可解释 | 原地重试与返港成本测试 | 通过 |
| 模拟器和设备编译通过 | arm64 双构建与 QA 运行 | 通过 |

## 5. 保留问题

- 真实玩家是否愿意支付补给换情报，需要阶段 4 观察而不是只看自动测试；
- 当前数值只服务首章样片，不能外推到十六海域；
- 战斗仍是功能原型，缺少阶段 3 船只、角色、炮击、登船和受击表现；
- 旧引擎 deployment target、LaunchImage、UIScene 与编译警告仍是发行前技术债。
